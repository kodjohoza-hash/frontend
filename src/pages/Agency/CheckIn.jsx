import CounterScannerPage from '../Counter/Scanner';

/**
 * Contrôle des billets & embarquement — espace compagnie (company_admin).
 * Réutilise l'interface de contrôle commune (scanner caméra, saisie manuelle,
 * vérification + embarquement via l'API réelle).
 */
const AgencyCheckIn = () => <CounterScannerPage />;

export default AgencyCheckIn;
