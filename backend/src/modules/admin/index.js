const routes = require('./routes');

/** Point d'entrée du module Administration (Module 19) : journal d'audit
 *  et paiements opérationnels globaux — réservé au super admin. */
module.exports = { routes };
