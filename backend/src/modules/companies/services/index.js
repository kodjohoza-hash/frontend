const companyService = require('./company.service');
const logoService = require('./logo.service');
const documentService = require('./document.service');

module.exports = { companyService, logoService, documentService };
