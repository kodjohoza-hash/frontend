const routes = require('./routes');
const services = require('./services');

/** Point d'entrée du module Notifications (système centralisé par utilisateur). */
module.exports = { routes, services };
